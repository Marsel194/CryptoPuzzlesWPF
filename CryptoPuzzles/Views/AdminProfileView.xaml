<UserControl x:Class="CryptoPuzzles.Views.AdminProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:helpers="clr-namespace:CryptoPuzzles.Helpers">
    <Grid Margin="24" MaxWidth="500">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Профиль администратора" FontSize="24" FontWeight="Bold" Margin="0,0,0,24"/>

        <TextBox Grid.Row="1" Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" 
                 materialDesign:HintAssist.Hint="Логин" Margin="0,0,0,16"/>

        <TextBox Grid.Row="2" Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" 
                 materialDesign:HintAssist.Hint="Имя" Margin="0,0,0,16"/>

        <TextBox Grid.Row="3" Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" 
                 materialDesign:HintAssist.Hint="Фамилия" Margin="0,0,0,16"/>

        <TextBox Grid.Row="4" Text="{Binding MiddleName, UpdateSourceTrigger=PropertyChanged}" 
                 materialDesign:HintAssist.Hint="Отчество" Margin="0,0,0,16"/>

        <PasswordBox Grid.Row="5" 
                     helpers:PasswordHelper.Attach="True" 
                     helpers:PasswordHelper.Password="{Binding NewPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     materialDesign:HintAssist.Hint="Новый пароль (оставьте пустым, если не меняете)" Margin="0,0,0,16"/>

        <PasswordBox Grid.Row="6" 
                     helpers:PasswordHelper.Attach="True" 
                     helpers:PasswordHelper.Password="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     materialDesign:HintAssist.Hint="Подтверждение пароля" Margin="0,0,0,24"/>

        <Grid Grid.Row="7">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Command="{Binding SaveCommand}" Content="Сохранить" 
                    Style="{StaticResource PrimaryButton}" Margin="4"/>
            <Button Grid.Column="1" Command="{Binding CancelCommand}" Content="Отмена" 
                    Style="{StaticResource MaterialDesignFlatButton}" Margin="4"/>
        </Grid>
    </Grid>
</UserControl>