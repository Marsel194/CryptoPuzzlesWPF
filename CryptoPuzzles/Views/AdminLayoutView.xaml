<UserControl x:Class="CryptoPuzzles.Views.AdminLayoutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:CryptoPuzzles.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель навигации -->
        <Border Background="{DynamicResource PrimaryHueMidBrush}" Padding="16">
            <StackPanel Orientation="Horizontal" Cursor="">
                <Button Command="{Binding NavigateBackCommand}" IsEnabled="{Binding CanGoBack}"
                        ToolTip="Назад" Style="{StaticResource MaterialDesignFlatButton}">
                    <materialDesign:PackIcon Kind="ArrowLeft" Foreground="{DynamicResource MaterialDesignBody}"/>
                </Button>
                <Button Command="{Binding NavigateForwardCommand}" IsEnabled="{Binding CanGoForward}"
                        ToolTip="Вперёд" Style="{StaticResource MaterialDesignFlatButton}">
                    <materialDesign:PackIcon Kind="ArrowRight" Foreground="{DynamicResource MaterialDesignBody}"/>
                </Button>
                <Button Command="{Binding NavigateHomeCommand}" ToolTip="На главную"
                        Style="{StaticResource MaterialDesignFlatButton}">
                    <materialDesign:PackIcon Kind="Home" Foreground="{DynamicResource MaterialDesignBody}"/>
                </Button>
                <!-- Тема и выход (можно вынести в отдельную ViewModel или добавить сюда) -->
            </StackPanel>
        </Border>

        <!-- Контент -->
        <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}"/>
    </Grid>
</UserControl>
